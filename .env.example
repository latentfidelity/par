# Data directory (mount as Docker volume)
META_DIR=/data/meta

# MCP Gateway port
MCP_PORT=3100

# Optional: Bearer token for authentication (leave empty to disable)
# MCP_AUTH_TOKEN=your-secret-token

# Optional: Comma-separated CORS origins
# MCP_CORS_ORIGINS=http://localhost:3100,http://yourserver:3100
